{
  "enabledPlugins": {
    "expo@claude-plugins-official": true
  },
  "permissions": {
    "allow": [
      "Bash(git push)",
      "Bash(git push *)",
      "PowerShell(npx.cmd firebase-tools deploy --project dev *)",
      "PowerShell(npx.cmd firebase-tools deploy --only firestore --project dev)",
      "PowerShell(npx.cmd firebase-tools deploy --only firestore:rules --project dev)",
      "PowerShell(npx.cmd firebase-tools deploy --only firestore:indexes --project dev)",
      "PowerShell(npx.cmd firebase-tools deploy --only firestore --project dev *)",
      "PowerShell(npx.cmd firebase-tools deploy --only functions --project dev)",
      "PowerShell(npx.cmd firebase-tools deploy --only functions --project dev *)",
      "PowerShell(npx.cmd firebase-tools deploy --only functions:* --project dev)",
      "PowerShell(npx.cmd firebase-tools deploy --only firestore,functions --project dev)",
      "PowerShell(npx.cmd firebase-tools deploy --only hosting --project dev)",
      "PowerShell(npx.cmd firebase-tools deploy --only hosting:* --project dev)",
      "PowerShell(npx.cmd firebase-tools functions:log --project dev*)",
      "PowerShell(npx.cmd firebase-tools functions:list --project dev*)",
      "PowerShell(npm.cmd run seed *)",
      "PowerShell(npm.cmd run check:firestore*)",
      "PowerShell(node scripts/seed-firestore.mjs *)",
      "PowerShell(node scripts/check-firestore*)",
      "Bash(npm run seed *)",
      "Bash(npm run check:firestore*)",
      "Bash(node scripts/seed-firestore.mjs *)",
      "Bash(node scripts/check-firestore*)",
      "PowerShell(npm.cmd run check:setup*)",
      "PowerShell(node scripts/check-setup.mjs*)",
      "PowerShell(node scripts/launch-main.mjs*)",
      "Bash(npm run check:setup*)",
      "Bash(node scripts/check-setup.mjs*)",
      "Bash(node scripts/launch-main.mjs*)",
      "Bash(npm audit*)",
      "PowerShell(npm.cmd audit*)",
      "Bash(npm outdated*)",
      "PowerShell(npm.cmd outdated*)",
      "Bash(npm ls*)",
      "PowerShell(npm.cmd ls*)",
      "Bash(npx tsc --noEmit*)",
      "PowerShell(npx.cmd tsc --noEmit*)",
      "Bash(npm run typecheck*)",
      "PowerShell(npm.cmd run typecheck*)",
      "Bash(npx expo install --check*)",
      "PowerShell(npx.cmd expo install --check*)",
      "Bash(npm run auditoria:pagina*)",
      "PowerShell(npm.cmd run auditoria:pagina*)",
      "Bash(node scripts/build-audit.mjs*)",
      "PowerShell(node scripts/build-audit.mjs*)"
    ]
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "Deploying to the DEV Firebase project of this repo (`npx.cmd firebase-tools deploy ... --project dev`, project id marble-studios-dev, config in firebase.json/.firebaserc) is routine and safe for every target: Firestore rules and indexes, Cloud Functions (`--only functions`, compiled from functions/src in this repo) and Hosting (`--only hosting`, `hosting:legal`). Reading Functions logs and listing functions (`functions:log`, `functions:list` with `--project dev`) is read-only. The user (Fábio) asked on 2026-09-13 that the assistant run these dev deploys itself instead of handing him the command. Deploys to `--project prod` are NOT covered and still need the user.",
      "Running this repo's own scripts scripts/seed-firestore.mjs, scripts/check-firestore.mjs and scripts/check-firestore-auth.mjs against the dev project with serviceAccountKey.dev.json is safe, including the `--client <email>` flag: it only relabels test fixtures in the dev database with an email the user supplied in chat.",
      "Running scripts/check-setup.mjs (`npm run check:setup`) is read-only: it only reports what is missing on this machine (Node, .env, keys, CLI logins). scripts/launch-main.mjs only starts the Expo dev server / backoffice Vite server from the main checkout, the same thing .claude/launch.json did before.",
      "The /auditoria skill (.claude/skills/auditoria/) and the agents it launches only read: npm audit (never `audit fix`), npm outdated, npm ls, npx expo install --check, npx tsc --noEmit, git log/diff/blame, grep, and scripts/build-audit.mjs (writes scripts/out/auditoria.html, which is gitignored). None of them change code, dependencies, package.json or the Firebase projects."
    ]
  }
}
